#!/usr/bin/env python

import setuptools

setuptools.setup(
    name='dep_injector',
    version='1.0.0',
    packages=setuptools.find_packages(),
    author='Jeremy Mikkola',
    description='A dependency injector backend',
    install_requires=[],
)
